apiVersion: config.lighthouse.jenkins-x.io/v1alpha1
kind: TriggerConfig
spec:
  presubmits:
    - name: secrets
      context: "secrets"
      always_run: true
      optional: false
      trigger: /gitleaks
      rerun_command: /gitleaks
      source: "secrets.yaml"
  postsubmits:

